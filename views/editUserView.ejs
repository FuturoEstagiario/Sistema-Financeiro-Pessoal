<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar-Usuario</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; }
        form { margin-top: 20px; }
    </style>
</head>
<body>
    <h1>Editar usuário</h1>

    <form action="/update/<%= user.id_user %>" method="post">
        <input type="text" name="nome_completo" placeholder="Nome completo do usuário" value="<%= user.nome_completo %>" required>
        <input type="text" name="login" placeholder="Login do usuário" value="<%= user.login %>" required>
        <input type="password" name="senha" placeholder="Senha(não é necessário alterar)">
        <select name="user_status" id="user_status" required>
            <option value="1" <%= user.user_status == 1 ? 'selected' : '' %>>Ativo</option>
            <option value="0" <%= user.user_status == 0 ? 'selected' : '' %>>Inativo</option>
        </select>
        <button type="submit">Atualizar</button>
    </form>
    <br>
    <a href="/users"><button type="button">Cancelar</button>
    </a>
</body>
</html>